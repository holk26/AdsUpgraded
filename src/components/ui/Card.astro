---
export interface Props {
  class?: string;
  variant?: 'default' | 'glass' | 'border';
}

const { 
  class: className = '',
  variant = 'default',
} = Astro.props;

const variants = {
  default: 'bg-white border border-brand-border shadow-sm',
  glass: 'bg-white/80 backdrop-blur-sm border border-brand-border shadow-sm',
  border: 'bg-bg-surface border-2 border-brand-primary',
};

const classes = `rounded-saas p-6 transition-all duration-200 hover:shadow-md ${variants[variant]} ${className}`;
---

<div class={classes}>
  <slot />
</div>
