---
import Button from './ui/Button.astro';

export interface Props {
  lang?: string;
  t: (key: string) => string;
}

const { lang = 'en', t } = Astro.props;

const navItems = [
  { key: 'nav.features', href: '#features' },
  { key: 'nav.pricing', href: '#pricing' },
  { key: 'nav.about', href: '#about' },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-md border-b border-gray-200 shadow-sm">
  <div class="container mx-auto px-4">
    <nav class="flex items-center justify-between py-4" aria-label="Main navigation">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 group focus-visible:outline-2 focus-visible:outline-brand-accent rounded-lg">
        <svg class="w-8 h-8 text-brand-primary transition-transform group-hover:scale-110" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
        </svg>
        <span class="text-xl font-bold text-text-heading">LeadFlow Engine</span>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center gap-8">
        {navItems.map((item) => (
          <a 
            href={item.href} 
            class="text-text-body hover:text-brand-primary transition-colors font-medium focus-visible:outline-2 focus-visible:outline-brand-accent rounded px-2 py-1"
          >
            {t(item.key)}
          </a>
        ))}
      </div>

      <!-- Actions -->
      <div class="flex items-center gap-3">
        <!-- Language Selector -->
        <div class="flex gap-2" role="group" aria-label="Language selection">
          <a 
            href="/" 
            class={`px-3 py-1.5 rounded-md font-medium transition-all ${lang === 'en' ? 'bg-brand-primary text-white shadow-sm' : 'text-text-muted hover:text-text-heading hover:bg-gray-100'}`}
            aria-label="Switch to English"
            aria-current={lang === 'en' ? 'true' : undefined}
          >
            EN
          </a>
          <a 
            href="/es" 
            class={`px-3 py-1.5 rounded-md font-medium transition-all ${lang === 'es' ? 'bg-brand-primary text-white shadow-sm' : 'text-text-muted hover:text-text-heading hover:bg-gray-100'}`}
            aria-label="Cambiar a Español"
            aria-current={lang === 'es' ? 'true' : undefined}
          >
            ES
          </a>
        </div>
        <Button variant="primary" size="sm" href="#demo" ariaLabel={t('nav.demo')}>
          {t('nav.demo')}
        </Button>
      </div>
    </nav>
  </div>
</header>

<!-- Skip to main content link for accessibility -->
<a 
  href="#main-content" 
  class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-brand-primary text-white px-4 py-2 rounded-lg z-[100] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-accent"
>
  Skip to main content
</a>
